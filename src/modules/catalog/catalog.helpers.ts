/**
 * catalog.helpers.ts
 *
 * Re-exports the shared handleError for backward compatibility.
 * Catalog routes now import directly from '../../lib/handle-error'.
 */
export { handleError } from '../../lib/handle-error';
